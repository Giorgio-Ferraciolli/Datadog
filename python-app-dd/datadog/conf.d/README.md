# Coloque aqui arquivos de configuração customizados para o Datadog Agent.
# Exemplo: postgres.d/conf.yaml, redis.d/conf.yaml etc.
# Esta pasta é montada em /etc/datadog-agent/conf.d dentro do container do Agent.
