init_config:

instances:

  - server: ${ORACLE_HOST}
    port: ${ORACLE_PORT}
    service_name: ${ORACLE_SERVICE}

    username: ${ORACLE_USER}
    password: ${ORACLE_PASSWORD}

    tags:
      - env:${ENVIRONMENT}
      - project:${PROJECT}
      - db:oracle-xe
